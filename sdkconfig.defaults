# Enable the NimBLE host stack (esp32-nimble builds on it).
CONFIG_BT_ENABLED=y
CONFIG_BT_BLE_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y

# The default main-task stack is tight once the BLE host is running.
CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192
